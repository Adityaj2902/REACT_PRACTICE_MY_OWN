// Two Point Must to be remember that whenever we create component function then function name must be start with Capital letter and must return some markpup .

// A compoenent is contain data login and apperance

// Jsx is an extension of javascript that allows us to write html in javascript

// It convert the jsx into javascript by using babel

// Each JSX element is just syntactic sugar for calling React.createElement(component, props, ...children) function.
